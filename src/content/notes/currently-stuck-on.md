---
title: "What I'm currently stuck on"
date: 2026-06-27
kind: stuck-on
tags: ['cuda', 'rnn', 'llm']
---

The three things eating most of my attention right now:

- **CUDA acceleration.** Getting from "it runs on the GPU" to "it uses the GPU
  well" is a different skill, and I'm still building the intuition for where the
  real bottlenecks are versus where I assume they are.
- **Sequence models (RNN / LSTM).** I can use them; I want the deeper intuition
  for *why* a given architecture behaves the way it does on a given sequence,
  not just which knob to turn.
- **Production LLM / RAG pipelines.** The gap between a notebook demo and a
  system that's cost-bounded, observable, and honest when it doesn't know — that
  gap is most of the work, and the most interesting part.

> _Seed entry — replace with your real current notes as the work moves._
