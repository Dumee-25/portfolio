{
  "name": "dumindu-portfolio",
  "type": "module",
  "version": "1.0.0",
  "description": "Portfolio for Dumindu Kumarapeli — an evidence-machine that lives by the standard it preaches.",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro check && astro build",
    "preview": "npm run build && wrangler dev",
    "astro": "astro",
    "generate-types": "wrangler types",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/cloudflare": "^14.1.4",
    "@fontsource-variable/inter": "^5.2.8",
    "@fontsource-variable/source-serif-4": "^5.2.9",
    "@fontsource/ibm-plex-mono": "^5.2.7",
    "astro": "^7.0.3",
    "typescript": "^5.7.0"
  },
  "devDependencies": {
    "wrangler": "^4.113.0"
  }
}
